---
title: 通过MCSManger面板部署MC服务器
date: '2026-07-18 19:11:50'
tags: []
mood: ''
cover: https://img.cdn1.vip/i/6a5b996215a71_1784387938.webp
description: ''
---

‍

# 一、准备环境

•一台电脑/服务器(Windows或Linux)

•内网穿透工具(有公网IP请忽略)

# 二、Windows系统使用MCSManger

## 1.安装面板

前往[MCSManger下载页面](https://cdn.imlazy.ink:233/files/mcsmanager_windows_release.zip) 下载最新版压缩包

解压后的文件系统大概是这样

```
mcsmanager/
├── daemon/                          # 守护进程 - 管理进程/IO/定时任务
│   ├── app.js                       # 入口文件 (编译后)
│   ├── app.js.map                   # Source map
│   ├── node_app.exe                 # Node.js 运行时 (嵌入式)
│   ├── package.json                 # 依赖: Koa, Socket.IO, Docker, etc.
│   ├── package-lock.json
│   ├── node_modules/                # 依赖包
│   └── lib/                         # 平台原生二进制工具
│       ├── 7z_* / file_zip_*        # 压缩工具 (win/linux/mac, arm/x64)
│       └── pty_*                    # 伪终端 (PTY) 支持
├── web/                             # Web 面板 - 用户界面 + API
│   ├── app.js                       # 入口文件 (编译后)
│   ├── app.js.map                   # Source map
│   ├── node_app.exe                 # Node.js 运行时
│   ├── package.json                 # 依赖: Koa, Socket.IO, Redis, bcrypt, OTP等
│   ├── package-lock.json
│   ├── node_modules/
│   └── public/                      # 前端静态资源
│       ├── assets/                  # 资源文件
│       ├── index.html               # 主页面
│       ├── stats.html               # 统计页面
│       ├── favicon.ico
│       └── robots.txt
├── start.bat                        # 启动脚本 (启动daemon + web)
└── LICENSE                          # Apache-2.0
```

双击运行start.bat，等待启动成功后，会弹出这样的一个网页

  
‍![](https://img.cdn1.vip/i/6a5b25fbcb796_1784358395.webp)

如果没有出现这个页面，请手动打开浏览器访问[MCSManger管理后台](http://localhost:23333/) 并根据指引完成管理员账号、密码的设置

完成后会出现这个提示

  
‍![](https://img.cdn1.vip/i/6a5b26bee61c1_1784358590.webp)

请选择"老用户"后将进入后台面板

## **2.配置公网访问(非必要)**

**声明：配置公网访问会将面板暴漏在公网，请设置安全性较高的密码。出现问题我概不负责**

在导航栏中点击'**节点**'按钮，进入节点配置页面

  
‍![](https://img.cdn1.vip/i/6a5b2d14a0af2_1784360212.webp)

点击'**新增节点**'，具体配置如下

  
‍![](https://img.cdn1.vip/i/6a5b305a1a74f_1784361050.webp)

### ***如何获取远程节点密钥****：*

进入

```
你的MCSManger安装目录\daemon\data\Config
```

例如

```
F:\Users\38592\Downloads\mcsmanager\daemon\data\Config
```

打开**global.json** 找到类似这样的一行

```
"key": "f7f0eecad9a344dc76b3d2aef1849379fa406ed3c25a715"
```

后面的

```
f7f0eecad9a344dc76b3d2aef1849379fa406ed3c25a715
```

就是你的远程节点密钥

配置完成后，你的节点管理页面大概会是这样

  
‍![](https://img.cdn1.vip/i/6a5b37c37c00e_1784362947.webp)

这个版本问题是我的问题，如果你按照这个教程正常部署，不会出现这个问题

# 三、Linux系统使用MCSManger

## 1.安装MCSManger

这里有3种方法安装(我比较推荐第三种方法)

### *1.通过Docker Compose安装*

```
# docker-compose.yml
services:
  web:
    image: githubyumao/mcsmanager-web:latest
    ports:
      - "23333:23333"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - <CHANGE_ME_TO_INSTALL_PATH>/web/data:/opt/mcsmanager/web/data
      - <CHANGE_ME_TO_INSTALL_PATH>/web/logs:/opt/mcsmanager/web/logs

  daemon:
    image: githubyumao/mcsmanager-daemon:latest
    restart: unless-stopped
    ports:
      - "24444:24444"
    environment:
      - MCSM_DOCKER_WORKSPACE_PATH=<CHANGE_ME_TO_INSTALL_PATH>/daemon/data/InstanceData
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - <CHANGE_ME_TO_INSTALL_PATH>/daemon/data:/opt/mcsmanager/daemon/data
      - <CHANGE_ME_TO_INSTALL_PATH>/daemon/logs:/opt/mcsmanager/daemon/logs
      - /var/run/docker.sock:/var/run/docker.sock
```

所有的**需要替换为实际存放数据**的位置

命令行**依次**执行

```
mkdir -p <CHANGE_ME_TO_INSTALL_PATH> # <CHANGE_ME_TO_INSTALL_PATH>需要替换为实际存放数据的位置
cd <CHANGE_ME_TO_INSTALL_PATH> # <CHANGE_ME_TO_INSTALL_PATH>需要替换为实际存放数据的位置
vim docker-compose.yml # 这里写入上面的docker-compose.yml的内容
docker compose pull && docker compose up -d
```

**注意！！存放数据的位置需要被持久化存储，否则重启容器将丢失数据**

### *2.通过命令行安装(Docker)*

第一步 拉取镜像

```
docker pull githubyumao/mcsmanager-daemon:latest
docker pull githubyumao/mcsmanager-web:latest
```

如果拉取失败或拉取速度太慢，可自行配置镜像加速

第二步 ~~原神~~MCSManger启动！！

以下命令中的${CHANGE\_ME\_TO\_INSTALL\_PATH}**需要改成实际存储数据的位置**

```
# 启动 MCSManager 守护进程端
docker run -v /etc/timezone:/etc/timezone:ro  \
-v /etc/localtime:/etc/localtime:ro  \
-v ${CHANGE_ME_TO_INSTALL_PATH}/daemon/data:/opt/mcsmanager/daemon/data \
-v ${CHANGE_ME_TO_INSTALL_PATH}/daemon/logs:/opt/mcsmanager/daemon/logs \
-v /var/run/docker.sock:/var/run/docker.sock \
-e MCSM_DOCKER_WORKSPACE_PATH=${CHANGE_ME_TO_INSTALL_PATH}/daemon/data/InstanceData \
-p 24444:24444 \
-d githubyumao/mcsmanager-daemon:latest

# 启动 MCSManager Web 端
docker run \
-v /etc/localtime:/etc/localtime:ro \
-v ${CHANGE_ME_TO_INSTALL_PATH}/web/data:/opt/mcsmanager/web/data \
-v ${CHANGE_ME_TO_INSTALL_PATH}/web/logs:/opt/mcsmanager/web/logs \
-p 23333:23333 \
-d githubyumao/mcsmanager-web:latest
```

**注意！！存放数据的位置需要被持久化存储，否则重启容器将丢失数据**

### *3.一键脚本安装MCSManger(不使用Docker)🌟推荐*

命令行执行

```
sudo su -c "wget -qO- https://script.mcsmanager.com/setup_cn.sh | bash"
```

提示安装成功后的使用方法：

启动Web端和守护进程

```
systemctl start mcsm-{web,daemon}
```

关闭Web端和守护进程

```
systemctl stop mcsm-{web,daemon}
```

仅启动守护进程(用于多节点的用户)

```
systemctl disable mcsm-web
```

## 2.配置MCSManger

Linux系统和Windows系统的配置步骤相差不大，但是略有一些区别

安装完成后，如果使用的是云服务器，请先**放行端口23333和24444**

使用浏览器访问**http://<你的公网IP/域名>:23333**，你会看到

  
‍![](https://img.cdn1.vip/i/6a5b25fbcb796_1784358395.webp)

根据指引完成面板配置后，请选择"**老用户**"按钮，然后点击"**节点**"，进入"**节点配置**"页面

你可能会看到这样一个节点

  
‍![](https://img.cdn1.vip/i/6a5b5085073cf_1784369285.webp)

如果**网页直连**的状态**正常**，那么就可以忽略以下内容

如果**网页直连**的状态**异常**，那么就**必须**按照以下方法进行配置

点击'**新增节点**'，具体配置如下

  
‍![](https://img.cdn1.vip/i/6a5b305a1a74f_1784361050.webp)

### ***如何获取远程节点密钥****：*

进入

```
/你的MCSManger安装目录/daemon/data/Config
```

例如

```
/opt/mcsmanager/daemon/data/Config
```

打开**global.json** 找到类似这样的一行

```
"key": "f7f0eecad9a344dc76b3d2aef1849379fa406ed3c25a715"
```

后面的

```
f7f0eecad9a344dc76b3d2aef1849379fa406ed3c25a715
```

就是你的远程节点密钥

配置完成后，你的节点管理页面大概会是这样

  
‍![](https://img.cdn1.vip/i/6a5b37c37c00e_1784362947.webp)

这个版本问题是我的问题，如果你按照这个教程正常部署，不会出现这个问题

# 四、开服(这里以forge举例)

开服有两个方法：

第一个方法更加简单，但是不一定成功 第二个方法更折腾，但是成功率更高

### ***方法一*：使用内置模板**

点击面板主页右上角的"**新建应用**"

向下拉，点击**Minecraft**

  
‍![](https://img.cdn1.vip/i/6a5b5551384c6_1784370513.webp)

选择一个节点，再选择你想要的版本，点击安装即可

  
‍![](https://img.cdn1.vip/i/6a5b55ed72e47_1784370669.webp)

然后它会**全流程自动部署**，部署后只需要根据指引启动服务器并**放行对应端口(一般是25565)**就可以~~原神~~服务器启动了

### *方法二：*手动部署

点击面板主页右上角的"**新建应用**"，选择"**直接创建**"

进入创建的实例，点击"**文件管理**"

前往[FastMirror镜像站](https://www.fastmirror.net/#/download/Paper?coreVersion=26.2) 下载对应版本的核心，这里以1.20.1forge举例

下载完成后上传到实例根目录

  
‍![](https://img.cdn1.vip/i/6a5b5b59b9398_1784372057.webp)

然后返回实例控制台，进入"**java环境管理**"下载合适的java

接下来进入"**应用实例设置**" 这步很重要！

具体操作如下

  
‍![](https://img.cdn1.vip/i/6a5b5ec1d1d04_1784372929.webp)

最后根据你自己设置的端口号开放防火墙对应端口